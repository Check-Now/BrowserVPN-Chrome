# Uninstall

Run:

```powershell
powershell -ExecutionPolicy Bypass -File installer\scripts\uninstall-host.ps1
```

If installed through Inno Setup, uninstall `BrowserVPN-Chrome Native Host` from Windows Settings.

The uninstall script removes only the current-user Native Messaging registry key.
